# integration

**Note:** :tests:integration — cross-module integration and device tests: RuntimeExecutionTest, PluginExecutionTest, DependencyInstallationTest, ProcessCancellationTest.

See `/STRUCTURE.md` for placement, package, naming, and dependency rules.
