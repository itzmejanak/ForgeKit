# core

Group: pure-Kotlin domain libraries. Modules: common, model, database, logging, security, filesystem.

See `STRUCTURE.md` section 4 for the module registry, dependency edges, and activation stages.
