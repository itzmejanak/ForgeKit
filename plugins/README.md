# plugins

Group: plugin platform. Modules: api, manifest, installer, validator, resolver, protocol, manager, registry.

See `STRUCTURE.md` section 4 for the module registry, dependency edges, and activation stages.
