# patches

**Note:** Diff patches applying ForgeKit integration changes on upstream Termux. One file per change with upstream commit reference and build instructions.

See `/STRUCTURE.md` for placement, package, naming, and dependency rules.
