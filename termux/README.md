# termux

Group: embedded Termux integration. Areas: upstream, patches, bootstrap (non-Gradle), embedded (Gradle :termux:embedded).

See `STRUCTURE.md` section 4 for the module registry, dependency edges, and activation stages.
