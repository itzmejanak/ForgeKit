# bootstrap

**Note:** Architecture-specific Termux bootstrap archives and build scripts (minimum arm64-v8a). Descriptors are referenced by runtime/bootstrap.

See `/STRUCTURE.md` for placement, package, naming, and dependency rules.
